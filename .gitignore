# Extension signing key — NEVER commit. Store outside the repo.
*.pem

# Packed build artifacts
*.crx
*.zip

# Dependencies / build output (for when there's a build step)
node_modules/
dist/
build/

# Generated dev tooling (regenerate with `npm run doug`)
dev/doug-sheet.html

# Editor / OS
.vscode/
.idea/
Thumbs.db
desktop.ini
*.log

# Browser test output and local visual review captures
test-results/
playwright-report/
docs/review/
__pycache__/

# Local credentials and browser/profile data
.env
.env.*
!.env.example
*.key
*.p12
*.pfx
*.jks
*.keystore
*.xpi
.npmrc
.web-ext-config.*
web-ext-artifacts/
playwright/.auth/
*.sqlite
*.sqlite3
*.har
